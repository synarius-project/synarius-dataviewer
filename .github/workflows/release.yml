name: Release

on:
  push:
    tags:
      - "v*"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - run: pip install build
      - run: python -m build
      - uses: actions/upload-artifact@v5
        with:
          name: dist-python
          path: dist/*

  windows-installer:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Set app version from release tag
        shell: powershell
        run: |
          $tag = "${env:GITHUB_REF_NAME}"
          if (-not $tag.StartsWith("v")) {
            throw "Expected tag format vX.Y.Z, got: $tag"
          }
          $version = $tag.Substring(1)
          if ($version -notmatch '^\d+\.\d+\.\d+$') {
            throw "Tag must match vX.Y.Z. Got: $tag"
          }
          "__version__ = `"$version`"" | Out-File -Encoding utf8 src/synarius_dataviewer/_version.py
      - run: pip install . pyinstaller
      - name: PyInstaller (one-file console app + synarius_core)
        run: pyinstaller --noconfirm --clean synarius_dataviewer.spec
      - run: choco install wixtoolset -y
      - name: Build MSI with WiX
        shell: powershell
        run: |
          $tag = "${env:GITHUB_REF_NAME}"
          if (-not $tag.StartsWith("v")) {
            throw "Expected tag format vX.Y.Z, got: $tag"
          }
          $version = $tag.Substring(1)
          if ($version -notmatch '^\d+\.\d+\.\d+$') {
            throw "Tag must match vX.Y.Z for WiX MSI versioning. Got: $tag"
          }
          if (-not (Test-Path "DISCLAIMER.txt")) {
            throw "DISCLAIMER.txt not found in repository root."
          }
          $disclaimerText = Get-Content -Raw "DISCLAIMER.txt"
          $escapedText = $disclaimerText.Replace('\', '\\').Replace('{', '\{').Replace('}', '\}').Replace("`r`n", '\par ').Replace("`n", '\par ')
          "{\rtf1\ansi\deff0 {\fonttbl {\f0 Arial;}}\f0\fs20 $escapedText}" | Out-File -Encoding ascii disclaimer.rtf
          @"
          <?xml version="1.0" encoding="UTF-8"?>
          <Wix xmlns="http://schemas.microsoft.com/wix/2006/wi">
            <Product Id="*" Name="synarius-dataviewer" Language="1033" Version="$version" Manufacturer="Synarius Contributors" UpgradeCode="A91F3E2D-8B4C-4F6A-9D1E-7C0B5A384726">
              <Package InstallerVersion="500" Compressed="yes" InstallScope="perMachine" />
              <MediaTemplate EmbedCab="yes" />
              <WixVariable Id="WixUILicenseRtf" Value="disclaimer.rtf" />
              <Property Id="WIXUI_INSTALLDIR" Value="INSTALLFOLDER" />
              <Property Id="WIXUI_EXITDIALOGOPTIONALCHECKBOXTEXT" Value="Launch synarius-dataviewer" />
              <Property Id="WixShellExecTarget" Value="[#MainExeFile]" />
              <Feature Id="MainFeature" Title="synarius-dataviewer" Level="1">
                <ComponentRef Id="MainExeComponent" />
                <ComponentRef Id="StartMenuShortcutComponent" />
              </Feature>
              <UIRef Id="WixUI_InstallDir" />
              <UIRef Id="WixUI_ErrorProgressText" />
              <CustomAction Id="LaunchApplication"
                            BinaryKey="WixCA"
                            DllEntry="WixShellExec"
                            Impersonate="yes" />
              <UI>
                <Publish Dialog="ExitDialog"
                         Control="Finish"
                         Event="DoAction"
                         Value="LaunchApplication">WIXUI_EXITDIALOGOPTIONALCHECKBOX = 1 AND NOT Installed</Publish>
              </UI>
            </Product>
            <Fragment>
              <Directory Id="TARGETDIR" Name="SourceDir">
                <Directory Id="ProgramFilesFolder">
                  <Directory Id="INSTALLFOLDER" Name="synarius-dataviewer">
                    <Component Id="MainExeComponent" Guid="*">
                      <File Id="MainExeFile" Source="dist\synarius-dataviewer.exe" KeyPath="yes" />
                    </Component>
                  </Directory>
                </Directory>
                <Directory Id="ProgramMenuFolder">
                  <Directory Id="ApplicationProgramsFolder" Name="synarius-dataviewer" />
                </Directory>
              </Directory>
            </Fragment>
            <Fragment>
              <DirectoryRef Id="ApplicationProgramsFolder">
                <Component Id="StartMenuShortcutComponent" Guid="*">
                  <Shortcut Id="ApplicationStartMenuShortcut"
                            Name="synarius-dataviewer"
                            Description="Start synarius-dataviewer"
                            Target="[INSTALLFOLDER]synarius-dataviewer.exe"
                            WorkingDirectory="INSTALLFOLDER" />
                  <RemoveFolder Id="ApplicationProgramsFolderRemove" On="uninstall" />
                  <RegistryValue Root="HKCU"
                                 Key="Software\Synarius\synarius-dataviewer"
                                 Name="StartMenuShortcut"
                                 Type="integer"
                                 Value="1"
                                 KeyPath="yes" />
                </Component>
              </DirectoryRef>
            </Fragment>
          </Wix>
          "@ | Out-File -Encoding UTF8 installer.wxs
          candle.exe installer.wxs
          light.exe installer.wixobj -ext WixUIExtension -ext WixUtilExtension -o dist\synarius-dataviewer-installer.msi
      - uses: actions/upload-artifact@v5
        with:
          name: dist-windows-installer
          path: dist/synarius-dataviewer-installer.msi

  release-assets:
    runs-on: ubuntu-latest
    needs: [build, windows-installer]
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v5
        with:
          path: release-assets
      - uses: softprops/action-gh-release@v2
        with:
          files: release-assets/dist-windows-installer/synarius-dataviewer-installer.msi
